The data has only one dataset under Data

Make sure featurewiz() is installed before running main.ipynb
Please run the following command in the terminal:

pip install featurewiz

If it prompts that there is an uninstalled program, please follow the instructions to install the corresponding package
Then run all the code directly in Jupyter Notebook